// Member access

(const z 3.1415)

// Method call
(const formatted (.toFixed z 2))
(log formatted) // => 3.14

// get property
(log (get "length" formatted)) // => 4
(log (get "length" [1,2,3])) // => 3

// get is auto carried
(const len (get "length"))
(log (len [1,2,3,4]))